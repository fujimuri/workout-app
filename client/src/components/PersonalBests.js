import React, { useState, useEffect} from 'react';

function PersonalBests(props) {
    // display a list of PBs for each lift for this user
    // (only for lifts that the user has ever attempted)
    return (
        <div>
            Personal Bests
        </div>
    )
}